Hello! I am a backend Python (Django/FastAPI) developer who is currently taking an intensive, interactive 4-Hour Python-to-Go Crash Course.

Here is our current progress:
- **Workspace Context**: I have a `crash-course` directory with a `go.mod` file initialized and `gin-gonic/gin` downloaded.
- **Concepts We Have Mastered**:
  - **Hour 1**: `go mod`, variables, arrays vs slices, maps, `for` loops, `range`, and `if err != nil` (error handling/wrapping).
  - **Hour 2**: Data Modeling. We ditched Python `class` to learn Structs, Receivers (methods), and Pointers `*` for pass-by-reference mutation.
  - **Hour 3**: Web APIs. We mapped Python's FastAPI to Go's `github.com/gin-gonic/gin`. We learned `GET`/`POST` routing, Struct Tags (replacing Pydantic BaseModels), and `c.ShouldBindJSON` with If-statement initialization (`if err := do(); err != nil`).

I am now completely ready for the final leg—**Hour 4: Concurrency Unleashed (Goroutines and Channels)**. 

Please provide the next `main.go` code snippet demonstrating:
1. Moving away from Python's `asyncio` and event loops to the incredible simplicity of the `go` keyword.
2. Using `sync.WaitGroup` to wait for background tasks.
3. Using Channels to safely pass data between running routines.
4. A grand finale challenge script that concurrently fetches multiple URLs!

Walk me through the differences between this and Python conceptually, exactly like my last assistant did!
