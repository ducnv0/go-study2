Hello! I am a backend Python (Django/FastAPI) developer who is currently taking an intensive, interactive 4-Hour Python-to-Go Crash Course. I recently switched computers and need you to continue my lesson seamlessly from where we left off.

Here is our current progress:
- **Files/Context**: We have a workspace with a `go_learning_plan.md` and `go_4_hour_crash_course.md` explaining Go vs Python concepts. I have a `crash-course` directory with a `go.mod` file initialized.
- **Concepts Completed**: We already learned `go mod` (replacing `venv`), `go build` vs `go run`, pointers, struct tags, variables (type inference vs explicit), typed Maps, Slices (`[]int`), and how `append` memory reallocation (Length vs Capacity) works under the hood.

We are currently finishing up **Hour 1**. We just looked at Variables, Slices, and Maps in `main.go`.

I am now ready for **Hour 1 - Step 3: Control Flow & Error Handling**. 

Please provide the next code snippet for my `main.go` file that demonstrates:
1. `for` loops (since I know Go has no `while` loop).
2. using `for index, value := range collection` to iterate over Slices/Maps.
3. Call a function that returns an error and show me the Python `try/except` replacement (`if err != nil`).

Please walk me through the code conceptually using Python comparisons, just like my previous assistant did!
